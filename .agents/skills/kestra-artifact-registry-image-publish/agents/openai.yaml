interface:
  display_name: "Kestra Artifact Registry Image Publish"
  short_description: "Publish Kestra routing images to Artifact Registry"
  default_prompt: "Use $kestra-artifact-registry-image-publish to build, publish, and verify the OSS worker routing Kestra image in Google Artifact Registry."
